version: '3.8'

services:
  # Redis service for session and file cache management
  redis:
    image: redis:7-alpine
    container_name: purrrr-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: >
      redis-server
      --maxmemory 2gb
      --maxmemory-policy allkeys-lru
      --appendonly yes
      --appendfsync everysec
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 5s
    networks:
      - purrrr-network
    restart: unless-stopped

  # purrrr Flask web application
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: purrrr-web
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - REDIS_URL=redis://redis:6379/0
      - UPLOAD_FOLDER=/tmp/purrrr
      - MAX_FILE_SIZE=500
      - SECRET_KEY=your-super-secret-key-change-in-production
    volumes:
      - ./logs:/app/logs
      - purrrr_temp:/tmp/purrrr
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - purrrr-network
    restart: unless-stopped

volumes:
  redis_data:
    driver: local
  purrrr_temp:
    driver: local

networks:
  purrrr-network:
    driver: bridge
